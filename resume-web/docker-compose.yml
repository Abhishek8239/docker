services:
  web:
    build: .
    ports:
      - "5001:5001"  # host:container
    environment:
      FLASK_APP: app
      FLASK_RUN_HOST: 0.0.0.0
      FLASK_RUN_PORT: 5001  # 👈 Flask will run on port 5001 inside the container
      FLASK_ENV: development

